import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    svgImage: {
        width: "100%",
        height: "100%",
        maxWidth: "100%",
        maxHeight: "100%",
        
    }
});

export default styles;